<a name="{{version}}"></a>
{{#if isPatch~}}
	##
{{~else~}}
	#
{{~/if}}
{{version}}
{{~#if title}}
	"{{title}}"
{{/if}}
{{~#if date}}

	🗓
	{{date}}
{{~/if}}
{{#if docsLink}}
	• 📑 [Documentation]({{docsLink}})
{{~/if}}
{{#if @root.linkCompare}}
	• 📝 [Commits](
	{{~#if @root.repository~}}
		{{~#if @root.host}}
			{{~@root.host}}/
		{{~/if}}
		{{~#if @root.owner}}
			{{~@root.owner}}/
		{{~/if}}
		{{~@root.repository}}
	{{~else}}
		{{~@root.repoUrl}}
	{{~/if~}}
	/compare/{{previousTag}}...{{currentTag}})
{{~/if}}