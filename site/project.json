{
	"name": "docs",
	"tags": ["tooling"],
	"implicitDependencies": ["tag:component", "ui-icons"],
	"namedInputs": {
		"core": [
			"{projectRoot}/data",
			"{projectRoot}/content",
			"{projectRoot}/resources"
		],
		"scripts": ["{projectRoot}/.eleventy.js"],
		"tools": [
			"{projectRoot}/_includes",
			"{projectRoot}/utils",
			"{projectRoot}/.eleventy.js"
		]
	},
	"targets": {
		"clean": {},
		"build": {
			"dependsOn": ["^build", "clean"],
			"inputs": ["core", "scripts", "tools"],
			"executor": "nx:run-commands",
			"options": {
				"cwd": "{projectRoot}",
				"commands": ["eleventy"]
			}
		},
		"start": {
			"depends": ["^build", "clean"],
			"inputs": ["core", "scripts", "tools"],
			"executor": "nx:run-commands",
			"options": {
				"cwd": "{projectRoot}",
				"commands": [
					"eleventy --serve --incremental",
					"nx watch  --projects=tag:components,ui-icons,tokens --includeDependentProjects -- nx build $NX_PROJECT_NAME"
				]
			}
		}
	}
}
