import { Template } from "./template";


/**
 * {{ description }}
 */
export default {
  title: "Components/{{ name }}",
  component: "{{ pascalCase name }}",
  argTypes: {
    size: {
      name: "Size",
      type: { name: "string", required: true },
      table: {
        type: { summary: "string" },
        category: "Component",
        defaultValue: { summary: "m" }
      },
      defaultValue: "m",
      options: ["s", "m", "l", "xl"],
      control: "select"
    },
  },
  args: {
    rootClass: "spectrum-{{ pascalCase name }}",
    size: "m",
  },
  parameters: {
    actions: {
      handles: []
    },
    status: {
      type: process.env.MIGRATED_PACKAGES.includes('{{ folderName }}') ? 'migrated' : 'legacy'
    }
  }
};

export const Default = Template.bind({});
Default.args = {};
