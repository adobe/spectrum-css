{
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "name": "{{ folderName }}",
    "tags": ["component"],
    "targets": {
        "build": {},
        "clean": {},
        "compare": {},
        "test": {
            "defaultConfiguration": "scoped"
        }
    }
}
