{
	"schema": "../../node_modules/nx/schemas/project-schema.json",
	"tags": ["component", "legacy"],
	"namedInputs": {
		"styles": ["{workspaceRoot}/index.css", "{workspaceRoot}/skin.css"]
	},
	"targets": {
		"clean": {
			"inputs": ["{workspaceRoot}/dist"],
			"outputs": []
		},
		"build": {
			"dependsOn": ["^build"],
			"inputs": ["styles"],
			"outputs": ["{workspaceRoot}/dist"]
		},
		"start": {
			"dependsOn": ["build", "^build"]
		}
	},
	"includedScripts": ["clean", "build", "start"]
}
