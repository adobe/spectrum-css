{
	"$schema": "../../node_modules/nx/schemas/project-schema.json",
	"name": "commons",
	"tags": ["utility"],
	"targets": {
		"build": {
			"executor": "nx:noop"
		},
		"clean": {
			"executor": "nx:noop"
		},
		"compare": {
			"executor": "nx:noop"
		},
		"format": {},
		"lint": {},
		"report": {
			"executor": "nx:noop"
		},
		"test": {
			"executor": "nx:noop"
		},
		"validate": {}
	}
}
