{
	"name": "picker",
	"tags": ["components"],
	"targets": {
		"build": {
			"dependsOn": ["^build"],
			"executor": "nx:run-commands",
			"options": {
				"cwd": "components/picker",
				"command": "gulp"
			}
		},
		"clean": {
			"executor": "nx:run-commands",
			"options": {
				"cwd": "components/picker",

				"command": "rimraf dist"
			}
		}
	}
}
