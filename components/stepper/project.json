{
	"name": "stepper",
	"tags": ["components"],
	"targets": {
		"build": {
			"dependsOn": ["^build"],
			"executor": "nx:run-commands",
			"options": {
				"cwd": "components/stepper",
				"command": "gulp"
			}
		},
		"clean": {
			"executor": "nx:run-commands",
			"options": {
				"cwd": "components/stepper",
				"command": "rimraf dist"
			}
		}
	}
}
