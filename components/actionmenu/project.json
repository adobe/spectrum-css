{
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "name": "actionmenu",
    "tags": ["component"],
    "targets": {
        "build": {
            "executor": "nx:noop"
        },
        "clean": {
            "executor": "nx:noop"
        },
        "format": {},
        "lint": {},
        "profile": {},
        "styles": {
            "executor": "nx:noop"
        },
        "test": {}
    }
}
