{
	"name": "typography",
	"tags": ["components"],
	"targets": {
		"build": {
			"dependsOn": ["^build"],
			"executor": "nx:run-commands",
			"options": {
				"cwd": "components/typography",
				"command": "gulp"
			}
		},
		"clean": {
			"executor": "nx:run-commands",
			"options": {
				"cwd": "components/typography",
				"command": "rimraf dist"
			}
		}
	}
}
